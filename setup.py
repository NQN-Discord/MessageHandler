from setuptools import setup


setup(
   name="message_helper",
   version="0.1.0",
   description="The set of regular expressions used for filtering out unwanted messages",
   author='Blue',
   url="https://nqn.blue/",
   packages=["message_helper"],
   install_requires=[]
)
